function arrayLength (arr) {
    for (var i = 0; i < arr.length; i++) {
        console.log(arr[i]);
    }
    console.log('Длина массива ' + arr.length);
}

arrayLength ([1,2]);
arrayLength ([1,2,34]);